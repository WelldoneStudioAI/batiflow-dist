Run `batiflow-os/03_REGRESSION_VALIDATION_PROTOCOL.md` against the change currently in the working
tree (`git status`, `git diff`).

Run the layer 1–3 checks for the change's risk tier, quote real command output, and produce the
Validation block from §6. Mark anything you could not run as NOT CHECKED with the exact command a
human must run. Never invent output.
