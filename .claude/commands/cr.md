Normalize the following into `batiflow-os/05_CHANGE_REQUEST_TEMPLATE.md` and echo the filled template
back, flagging every field you had to assume with `Assumed: …`. Then continue with the impact mapping
protocol (`batiflow-os/02_IMPACT_MAPPING_PROTOCOL.md`) before editing anything.

Request: $ARGUMENTS
